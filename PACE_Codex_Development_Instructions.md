# PACE 当前开发合同

本合同维护当前软件要求。数学定义统一引用 [FORMULA.md](docs/FORMULA.md)，
不再内嵌另一套公式附录。当前实现位于 `src/pace_livestock`，所有命令调用
同一套评分、校准、输入验证和输出代码。

## 必须保持的科学行为

1. 主估计对象为 bulk_proxy；逐检测层解析和拷贝边际平均先于活性计算。
2. 使用冻结、非重叠的规范评分单元、去重TSS和固定候选关系。
3. 固定活性检测层取等权几何均值；缺层不可临时改变panel，真实零保留。
4. 接触采用显式观测/先验策略；TSS权重固定，缺失不允许重新归一化权重。
5. eta为[0,1]中的连续参数。自动模式没有适用功能校准时使用0；
   有合格训练/校准数据时拟合，测试集不参与，后续分析可冻结复用。
6. eta=0直接跳过B；eta>0要求固定候选基因集合的必要联系齐全。
7. 主分母只有本基因实际可评分候选支持之和；总支持为零时结果NA。
   保留全部候选、真实零、技术缺失、实际分母和条件归一化状态。
8. RNA及附加组学默认注释；独立分类器分数与主公式分列，不相乘或平均。
9. 比较分析从支持重算共同分母，区分完整与条件差异。
10. 每次运行记录输入、模型、软件哈希、上下文、实际参数、种子和能力边界。

## 软件入口与输入边界

`PACE measured`、`PACE hybrid`、`PACE genome` 和 `PACE --mode ...` 支持
直接文件参数与YAML。安装、训练、准备和分析入口见[命令行](docs/cli.md)和
[软件手册](docs/software.md)。数据从合格的已处理轨道、接触和变异文件开始；
不把比对、峰识别、变异发现或未实现的研究扩展描述为已提供功能。

[数据字典](docs/data_dictionary.md)、[配置默认值](docs/parameters.md)、
[训练与校准](docs/training.md)、[模型限制](docs/limitations.md)是配套合同。
真实权重与相应背景的独立性能验证分别验收，不由合成测试推定。

## 变更验收

- 数值更改必须通过独立手算/解析解或科学不变量检验。
- 数据划分、参数适用范围、零/缺失处理和冻结制品复用需有行为测试。
- 三种模式必须能从安装后的PACE命令执行；构建包在仓库外验收。
- 首页、公式、参数、教程、例子与代码同步，文档检查纳入CI。
- 输入错误返回明确失败；已有输出不覆盖；不发布半成品成功目录。
- 模型或方法不兼容更改通过版本与迁移说明表达，历史仅通过Git追溯。

开发记录见[PLAN.md](PLAN.md)，实际检查见[验证记录](docs/validation.md)，
独立数学例见[审查与验算](PACE_Review_and_Validation.md)。
