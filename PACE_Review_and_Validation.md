# PACE 模型审查与验证清单

审查日期：2026-09-17。对象为《PACE：家养动物增强子—基因调控预测模型》及配套Codex开发指令。名称统一为PACE，不加模型代际编号。

## 1. 审查结论

主公式可保留，三种数据模式在具备相应输入和模型资产时有实现路径。此前草案的主要问题集中在估计对象、评分单元、归一化集合和验证边界，已在两份文件中修订。修订使方案更明确、可检验，不等于已经证明生物学性能提高。

本次完成文档与数学审查、独立手算、配置语法及两文件一致性核对。没有运行用户真实数据，没有训练真实家养动物权重，也没有实际安装/测试尚未生成的PACE软件；不能报告其预测准确率、训练耗时或优于其他模型的比例。

## 2. 实质问题与修复

| 优先级 | 原草案问题 | 为什么影响结果 | 本次处理 |
|---|---|---|---|
| 高 | bulk先合并信号与单倍型先算支持两种顺序混用 | 几何均值和乘积不满足交换聚合，输入相同也可输出不同份额 | 主输出固定bulk_proxy；拷贝支持总和作为不同研究估计对象 |
| 高 | 仅取共同输出边，未重算共同分母 | 技术缺失或eta导致分母缩小，可被误认成效应/性能提高 | 保存实际归一化集合，从support重算common score，完整和条件Delta分列 |
| 高 | 500bp序列目标与可变长合并区域混算 | 一个窗口预测不能代表任意长区域；重叠来源重复加分 | 统一非重叠规范评分单元、窗口ID和catalog；展示区域独立聚合 |
| 高 | 个体状态与群体均值的融合校准目标不清 | 其他动物的真实差异可能被误判为观测噪声，压低个体差异 | 校准制品声明individual_state/population_mean并检查测量设计 |
| 高 | 相对分数变化可能被当作增强子活性变化 | 其他元件变化即可改变该元件份额 | 联合报告Delta A、Delta support、gene total_support及Delta PACE |
| 高 | WGS未记录位置被隐含当可靠参考 | 变异文件本身不证明全基因组均可调用 | gVCF/callable BED或明确研究假设；缺失GT与参考分开 |
| 高 | “支持SV”未区分识别已报SV与发现SV | 普通SNV输入无法证明没有结构变异 | 仅标记输入已报告SV；未检测写not_assessed，不承担SV calling |
| 中高 | 预测/先验强制填写真实sample_id | 容易制造假样本及假重复 | 原观测与resolved evidence分表，预测用model/evidence ID |
| 中高 | 阳性标签未固定作用方向 | 非负增强支持可能与抑制/复杂作用混为同一任务 | 默认扰动后基因下降为增强阳性，上升及功效不足分别记录 |
| 中高 | 一次要求所有复杂研究模块完成 | 易产出大量半成品，真实权重缺失又被误当软件问题 | 三模式首发闭环与复杂扩展分开，软件/权重/任务验证分别验收 |
| 中高 | 位点预测、个体变化、E–G联系验证混用 | 一类准确率不能支持另外两类结论 | 三项验证状态独立；候选发现与联系阈值也分开 |
| 中 | CI训练少量轮次还要求达到发现准确率 | 易随随机种子/硬件波动，无科学代表性 | 固定模型验证数值路径，训练只检验梯度、更新、mask与保存加载 |

## 3. 独立数学反例

### 3.1 聚合顺序

E1两个单倍型ATAC/H3K27ac为(9,1)、(1,9)，E2为(4,4)、(4,4)，接触均1。

- 主bulk_proxy：先逐层平均，E1=(5,5)、E2=(4,4)，活性为5、4，E1分数 **5/9**。
- 每拷贝支持相加：E1为3+3=6，E2为4+4=8，E1分数 **3/7**。

二者都可定义，但估计对象不同；修订后不再把它们作为同一主分数的不同输入路径。

### 3.2 技术缺失改变分母

样本a支持为(1,1,2)，样本b为(1,1,NA)。原E1分数分别是1/4和1/2，直接相减得到的变化来自不同分母。

在共同E1/E2上从支持重算，两边都是(1/2,1/2)。这只是共同可测子集的条件比较；完整Delta仍为NA。不能把它解释为完整背景下“没有变化”。

### 3.3 组成份额不等于活性效应

S从(1,1)变为(1,2)，第一个元件支持不变，但份额从1/2降为1/3，Delta P=-1/6。若S从(1,1)变为(2,2)，总支持翻倍，而份额完全不变。

因此Delta PACE的正负不能单独证明增强子自身活性或基因表达改变方向。

### 3.4 原主公式与辅助计算

A=(4,2,1)，C_G1=(3,2,2)，C_G2=(1,2,6)：

| 目标 | eta=0 | eta=1 |
|---|---|---|
| G1 | (2/3,2/9,1/9) | (18/23,4/23,1/23) |
| G2 | (2/7,2/7,3/7) | (2/15,4/15,9/15) |

活性sqrt(4×9)=6；多TSS的(2,6)按(3/4,1/4)加权为3；log1p融合s=1、obs=9、seq=3、w=1/2得到sqrt(40)-1。CpG(1/2,8/8)的site均值0.75，reads加权比例0.9。

这些验算证明所列数学预期一致；软件生成后还须将其落实为测试，不能把文档验算称为软件已测试通过。

## 4. 可行性与实用性判断

| 能力 | 实现可行性 | 实际运行前提 |
|---|---|---|
| 实测公式模式 | 可从规范化表格直接实现 | 至少一个合法活动层、候选/TSS、接触或有来源先验 |
| H3K27ac-only | 保留，适合只有该活动层的数据 | 窗口和单位统一；不能在双层运行逐边偷换模式 |
| 混合模式 | 点估计和校准流程可实现 | 预测与实测匹配；真正融合需要对应目标的校准数据 |
| WGS已有候选预测 | 可实现atlas、参考序列与变异输入路径 | 真实定量权重、接触先验、注释及可调用性证据 |
| 个体差异可信预测 | 不能靠软件完成来证明 | 同位点跨动物/等位/变异扰动等独立验证 |
| 九组学扩展 | 提取和带标签学习可实现 | 没有功能标签时仅注释，不宣称全部进入主评分 |
| 全基因组新元件发现 | 研究扩展 | 候选分类/定量阈值、完整扫描评估及漏检统计 |
| 复杂CNV/SV物理支持 | 研究扩展 | 足够解析的结构、拷贝、坐标、单位和实验依据 |

首次发布应优先保证规范表格到结果的闭环、可靠错误信息、三种模式资产检查及可重复示例。已有仓库改造时使用显式profile保留既有区域分析，不能重写旧结果并声称可直接比较。

## 5. 科研验证应怎样展开

1. **实测起点**：以鸡肝脏等明确组织的H3K27ac/ATAC、Hi-C及RNA场景验证输入、候选和重复一致性。仅有两个动物时可检查重复一致性，但不足以证明跨品种泛化。
2. **模型机制**：固定评分单元、TSS与候选，比较距离、ABC-style、适用gABC、多TSS eta=0/1；按共同分母比较评分，并另报覆盖。
3. **缺失与低质量**：独立高质量目标作为评价参考；低深度实测、序列单来源、校准融合三者都比较，防止“放弃难样本”制造增益。
4. **WGS位点任务**：留出区域和动物，预测定量活动而不是仅峰概率；报告活性区、背景区、不同距离和可测性分层误差。
5. **WGS个体任务**：有配对数据后检验同一位点跨动物变化；参考模型对所有个体给同值是必要基线之一。没有这类数据时准确表述为个体序列计算流程完成。
6. **增强联系任务**：独立功能扰动、方向和功效规则明确；eQTL/QTL仅作辅助支持，不能把共定位当因果金标准。
7. **模型选择**：只有稳定改善并且覆盖/校准可接受时才改变推荐配置；没有增益时保留简单模型。

跨位点与个体效应是不同任务，多TSS和分配已有前人方法，相关边界见 [Sasse等](https://pubmed.ncbi.nlm.nih.gov/38036778/) 和 [gABC/STARE](https://github.com/SchulzLab/STARE)。PACE的科研价值应由上述可证伪比较建立，不由公式项数量或软件功能数量推断。

## 6. 本次交付与未完成事项

- 已修订模型说明、参数/局限表、Codex开发合同和验收预期，名称统一为PACE。
- 开发指令内嵌与独立模型文件完全一致的数学内容，避免开发者在两套定义之间选择。
- 首发任务与研究扩展边界明确；保留三个模式，不以复杂扩展阻塞基本可用软件。
- 仍需后续实际实现软件、准备真实权重与校准数据、完成适用背景的独立验证；本次没有用合成结果替代这些工作。
